KEEP MIGRATIONS

has to
#  improve with ngiNx static files (remove in django)
-> gunicon worker FOR BETTER PERFORMANCE

improve
# static in docker-compose
# get all environment to .env and setting instead of docker-compose


non now
# set up cicd to vps
